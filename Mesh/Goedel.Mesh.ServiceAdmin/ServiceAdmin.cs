//using Goedel.Mesh.ServiceAdmin;
//using Microsoft.Extensions.Logging;

//namespace Goedel.Logger;
//public sealed class ServiceAdmin : IServiceAdmin {
//    private readonly string _name;
//    private readonly Func<ServiceAdminConfiguration> _getCurrentConfig;

//    public ServiceAdmin(
//        string name,
//        Func<ServiceAdminConfiguration> getCurrentConfig) =>
//        (_name, _getCurrentConfig) = (name, getCurrentConfig);

//    public IDisposable BeginScope<TState>(TState state) => default!;



//    }